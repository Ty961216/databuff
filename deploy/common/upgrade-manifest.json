{
  "schema_version": 5,
  "upgrade_type": "A",
  "min_upgrade_from": "0.1.0",
  "notes": "V005 reduces dynamic-partition telemetry table buckets from 16 to 3 for newly created partitions (existing partitions unchanged until retention)."
}
